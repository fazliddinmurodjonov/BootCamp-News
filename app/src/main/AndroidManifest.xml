<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androiddatabaselesson2bootcamp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_pdp_uz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_pdp_uz"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDatabaseLesson2BootCamp">
        <activity
            android:name=".SplashActivity"
            android:label="Bootcamp news"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>